# Local-only config (contains real credentials for XAMPP dev)
config/local.php

# Composer dependencies — installed by the build (Railway/Fly/etc.)
/vendor/
composer.phar

# Fly.io local state
.fly/
fly.toml.bak

# Legacy local uploads dir — ID cards now live in DB as BLOBs,
# but XAMPP dev may still have leftover files on disk. Never commit them.
/uploads/

# Environment files
.env
.env.*
!.env.example

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log
